Run Django migrations.

Steps:
1. Run `uv run manage.py makemigrations` to create new migrations
2. Run `uv run manage.py migrate` to apply migrations
3. Report the results

If there are migration conflicts, help resolve them.
